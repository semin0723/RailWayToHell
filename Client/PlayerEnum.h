#pragma once
enum class PlayerPhase { Idle, Aim, Attack, MAX };
enum class AttackType { Normal, KnockBack };
enum class MoveType { Stand, Left, Right };
enum class WeaponType { Gun, Crossbow }; // normal, knockback?